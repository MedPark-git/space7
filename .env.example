# AI SPACE injects DB_HOST, DB_PORT, DB_NAME, DB_USER and DB_PASSWORD.
# Never commit real credentials.

PORT=3000
GOOGLE_CALENDAR_REDIRECT_URI=https://medprk-medpark-one.mycafe24.ai/api/calendar/oauth/callback

# Empty-database/bootstrap only. Remove after successful initialization/import.
INITIAL_ADMIN_PASSWORD=
AR_INITIAL_ADMIN_PASSWORD=
AR_MIGRATION_TOKEN=

# Optional. If omitted, calendar encryption remains compatible with DB_PASSWORD.
CALENDAR_SETTINGS_SECRET=
SECRET_KEY=
