{
  "name": "medpark-one-preview",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MedPark One internal portal",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
